{
  "name": "mine-app",
  "version": "1.0.0",
  "description": "Mine App - Meteor.js fullstack",
  "scripts": {
    "start": "meteor run --settings settings.json --port 3000"
  },
  "engines": {
    "node": "22.x"
  }
}
